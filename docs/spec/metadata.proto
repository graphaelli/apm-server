syntax = "proto3";
package model;

//import "google/protobuf/timestamp.proto";

service Apm {
    rpc SendEvents (Metadata) returns (EventsResponse) {}
}

message Metadata {
    message Service {
        string name = 1;
    }
}

message EventsResponse {}
